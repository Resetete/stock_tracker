<div class="row">
  <div class="col-10">
    <table class="table table-bordered table-hover">
      <thead>
        <tr>
          <th scope="col"> <%= pluralize(current_user.friends.count, 'Friend') %> </th>
          <th scope="col"> Last name </th>
          <th scope="col"> Number of tracked stocks </th>
          <th scope="col"> Actions </th>
        </tr>
      </thead>
      <tbody>
        <% @tracked_friends.each do |friend| %>
        <tr>
          <th scope="row"> <%= friend.first_name %> </th>
          <td> <%= friend.last_name %> </td>
          <td> <%= friend.stocks.count %> </td>
          <td>
            <%= link_to 'View profile', view_user_path(friend), class: 'btn btn-info btn-sm mr-3' %>
            <%= link_to 'Stop following', friendship_path(friend), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-warning btn-sm' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
